package com.example.petclinic;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Minimal Spring Boot entry point so tests can use {@code @SpringBootTest}
 * and {@code application.properties} without starting an embedded server.
 */
@SpringBootApplication
public class PetclinicApiTestsApplication {
}
